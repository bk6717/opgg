<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cos.opgg.repository.UserRepository">

<select id="findByUsername" resultType = "com.cos.opgg.model.User">
	SELECT * FROM post WHERE id = #{id}
</select>

<insert id="save">
	
</insert>
</mapper>